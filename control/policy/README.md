# Policy Surfaces

This directory holds shared policy surfaces for `codex_home`.

These files are intended to be:
- cross-repo
- deterministic-first
- reusable by local repo overlays without being re-authored per repo

Current policy surfaces:
- `token_burn_and_structured_output.md`
- `token_burn_and_structured_output.registry.json`

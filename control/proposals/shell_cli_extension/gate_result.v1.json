{
  "artifact_type": "proposal_gate_result",
  "artifact_version": "v1",
  "proposal_id": "shell-cli-extension-profile",
  "gate_status": "conditional_pass_with_underspecification",
  "result_summary": "The shell/CLI extension direction is coherent, grounded in the existing metadata model, and now demonstrated with a validated semantic profile example and validated projection binding example. The proposal should remain at the proposal register until compatibility and promotion posture are tightened for amendment.",
  "passes": [
    "existing metadata model is kept as the authority baseline",
    "shell assets are classified as projection assets",
    "implementation backends are classified as projection targets",
    "verification backends are classified as projection consumers",
    "shell policy and inventory are aligned with the projection-only posture",
    "a shell_cli extension profile has been defined against the existing canonical semantic model",
    "an example canonical semantic model for shell_cli validates against the existing metadata schema",
    "an example projection artifact manifest for shell_cli validates against the existing metadata schema"
  ],
  "underspecified_items": [
    "compatibility and promotion policy for introducing shell_cli profile semantics into the promoted metadata lane",
    "formal projection binding rules beyond the demonstrated Bashly and Bats examples, including argc, Argbash, and ShellSpec",
    "compatibility and extension impact against the current metadata model"
  ],
  "next_actions": [
    "classify compatibility impact for the shell_cli extension profile",
    "define additional backend projection examples as needed",
    "re-run proposal gating after the extension profile is explicit"
  ],
  "promotion_decision": "hold_at_proposal_register_pending_compatibility_classification"
}
